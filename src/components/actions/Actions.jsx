import React from 'react'

const Actions = () => {
  return (
    <div>
        {/**This will contain two buttons Del and Edit which will take the props
         * and then according the passing data it will filter the elements from the main context
         */}
    </div>
  )
}

export default Actions